<h1>Users</h1><%= link_to " (+)", new_user_path %>
<% @users.each do |user| %>
  <h3>Username: <%= link_to user.username, user_path(user) %></h3>
  <h4>Email: <%= user.email %></h4>
  <h4>Age: <%= user.age %></h4>
  <h4>Member Since: <%= user.created_at.strftime("%B %e, %Y") %></h4>
<% end %>
